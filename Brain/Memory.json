{
  "semantic": [
    {
      "key": "Sable scraper consolidation architecture",
      "value": "Scraper-mode memory consolidation sends prompt into active browser tab via scraper_service.chat() with stored chat_url, waits for model response, parses JSON, applies memory updates — unlike API mode which uses fire-and-forget backend Qwen calls. Frontend createChat() awaits /api/memory/consolidate-scraper in scraper mode before creating new chat."
    },
    {
      "key": "Sable chat_url storage",
      "value": "Browser chat URLs are stored in SQLite chats.chat_url column (auto-migrated). Saved after every scraper response via save_chat_url(). Retrieved via get_chat_url() to navigate scraper back to correct conversation instead of opening fresh empty chat."
    },
    {
      "key": "Sable scraper chat_id mismatch bug",
      "value": "When scraper_service.chat() receives a chat_id that differs from self.active_chat_id and no chat_url is provided, it opens a brand new browser chat via engine.new_chat() — causing messages to be sent into empty conversations. Fix: always pass chat_url when calling scraper_service.chat() for non-active chats."
    },
    {
      "key": "Sable consolidation endpoints",
      "value": "Two consolidation endpoints: POST /api/memory/consolidate (API mode, uses service.chat with parent_id threading) and POST /api/memory/consolidate-scraper (scraper mode, uses scraper_service.chat with chat_url navigation). Both parse JSON response and apply add/delete operations to Memory.json and Protected.json."
    },
    {
      "key": "Phantom bot project structure",
      "value": "phantom_bot at /home/sifat/hdd/projects/phantom_bot uses pyproject.toml (setuptools), entry in bot/main.py, config in config.py, core logic in bot/bot.py. Dependencies: python-telegram-bot>=21,<22, python-dotenv, requests, aiohttp, pymongo[srv], yt-dlp, telethon==1.44.0, fpdf2, pillow."
    },
    {
      "key": "Sable SkillParser _hold_start split-tag bug",
      "value": "SkillParser._hold_start() in engine/skills.py failed to hold partial tags split across SSE streaming chunks when attributes were mid-stream. The check `any(tag.startswith(partial))` only matched when partial was shorter than the tag name; once attributes arrived (e.g. 'view_file path=\"/home/...'), no known tag started with that string. Fix: added `or partial.startswith(tag)` to also match when the tag name has fully arrived but attributes are still streaming. Line ~1165."
    },
    {
      "key": "Sable background command final_log NameError bug",
      "value": "handle_execute_background_command in engine/skills.py referenced undefined variable `final_log` on lines 479 and 485 instead of `log_path`. Would cause NameError crash on every background job launch. Fixed by replacing both occurrences with `log_path`."
    },
    {
      "key": "Phantom bot webhook implementation",
      "value": "phantom_bot webhook support added to bot/main.py and config.py. Webhook activates only when USE_WEBHOOK=True AND IS_LOCAL!=True. Uses PTB v21 run_webhook() with bot token as URL path for security. Config vars: WEBHOOK_URL (public HTTPS domain), WEBHOOK_PORT (default 8443). Polling is default fallback for all other combinations. Health check server runs alongside in both modes via start_health_server()."
    },
    {
      "key": "Sable NEVER restart during active session",
      "value": "CRITICAL: Never run systemctl --user restart sable while Sifat is actively chatting through Sable. The conversation runs through Sable itself — restarting kills the connection mid-conversation. Fixes to skills.py or other engine files are saved to disk but only take effect on next natural/manual restart."
    },
    {
      "key": "phantom_bot regex rewriter double prefix bug",
      "value": "Regex rewriter (app.py ~L125) rewrites absolute paths like href='/static/...' by prepending url_prefix. When url_for already generated prefixed paths (via SCRIPT_NAME), the regex added another prefix, breaking CSS loads. Fix: exclude paths already starting with url_prefix by adjusting regex pattern to r'((?:href|action|src)\\s*=\\s*\")(/(?!/|{url_prefix.lstrip('/')}/))'."
    },
    {
      "key": "phantom_bot homepage timeout MongoDB cold-start",
      "value": "Index page (/ or /panel/) executes ~6 parallel MongoDB count queries on load. When Atlas cold-start delays first connection, all queries hang and proxy (aiohttp) times out (502). Not yet fixed; potential solutions: reduce query count, add timeout handling, or warm-up connection before request."
    },
    {
      "key": "phantom_bot JS hardcoded path fix pattern",
      "value": "To fix JS files with hardcoded absolute paths under a url_prefix: 1) Pass url_prefix to template via render_template('...', url_prefix=url_prefix or ''). 2) In template before script tag, set window.URL_PREFIX = '{{ url_prefix }}'. 3) In JS, const P = window.URL_PREFIX || '' and prepend P to all absolute URL strings."
    },
    {
      "key": "phantom_bot admin API user fields",
      "value": "/admin/api/users endpoint now returns user_id, section, and status (mapped from is_admin boolean). JS in admin_panel.html expects these fields for rendering sec and id columns."
    },
    {
      "key": "phantom_bot routine editor JS file location",
      "value": "Routine editor JavaScript is at /home/sifat/hdd/projects/phantom_bot/web/static/js/editor.js. Contains hardcoded API_LOAD_BASE='/routine/data/' and API_SAVE_URL='/routine/save' which break under url_prefix."
    },
    {
      "key": "phantom_bot schedule route variable mismatches",
      "value": "schedule_index route originally passed schedule_types and docs (dict), but template expected types and schedules (flat list). schedule_add route was /schedule/add/<stype> expecting subject/date/note, but template POSTs to /schedule/add with type/subject/teacher/date/time/topic/syllabus. Fixed by rewriting routes to match template expectations."
    }
  ],
  "episodic": [
    {
      "key": "Scraper consolidation empty response debug 2026-07-27",
      "value": "Scraper consolidation failed with 'no JSON object found' because the consolidation prompt was sent to a fresh empty browser chat instead of the active conversation. Root cause: missing chat_url parameter caused scraper to call engine.new_chat(). Fixed by adding chat_url column to chats table, storing URL after scraper responses, and passing it in consolidate-scraper endpoint."
    },
    {
      "key": "Sable parser and background bugs fixed 2026-07-27",
      "value": "Full audit of Sable engine/skills.py on 2026-07-27 found two bugs: (1) _hold_start split-tag failure causing tags streamed across SSE chunks to be released as plain text, (2) final_log NameError in handle_execute_background_command. Both fixed and syntax-verified. Server not restarted (active session). Frontend consumeChatStream, server SSE pipeline, editor_tools.py, and all handler functions audited clean."
    },
    {
      "key": "Phantom bot webhook added 2026-07-27",
      "value": "Added webhook support to phantom_bot on 2026-07-27. Modified config.py (WEBHOOK_URL, WEBHOOK_PORT), bot/main.py (run_local/run_webhook split), .env and .env.example. Condition: USE_WEBHOOK=True AND IS_LOCAL=False. Deploy target: Render with SSL auto-handled on .onrender.com domains."
    },
    {
      "key": "phantom_bot panel debugging marathon 2026-07-27",
      "value": "Debugged multiple issues with phantom_bot's panel under /panel url_prefix: admin panel redirect loop (hardcoded JS paths), schedule editor not loading (variable/form mismatches), CSS double prefix (regex rewriter), admin fields missing (API field mapping), routine editor not loading (hardcoded JS paths). All fixed except homepage timeout due to MongoDB Atlas cold-start on parallel count queries. Pattern emerged: any JS file with absolute fetch/redirect URLs must be prefixed-aware."
    }
  ],
  "procedural": [
    {
      "key": "Debugging scraper message delivery failures",
      "value": "When scraper messages fail silently (empty answer, no error), check: 1) Is chat_url being passed to scraper_service.chat()? Without it, mismatched chat_id triggers new_chat() instead of navigating to existing conversation. 2) Check stream_events logic around line 605 in engine/scraper.py where chat_id != self.active_chat_id branches. 3) Verify browser tab is alive via _is_browser_alive() check."
    },
    {
      "key": "Diagnosing phantom_bot failures",
      "value": "When phantom_bot is reported broken: 1) Check git log/status for uncommitted changes or recent bad commits. 2) Inspect bot/main.py entry point and config.py for import/config errors. 3) Verify .env exists and has required vars. 4) Try running with uv run python -m bot.main to catch runtime errors. 5) Check if dependencies need uv sync."
    },
    {
      "key": "Auditing Sable skill parser issues",
      "value": "When Sable skill tags fail intermittently: 1) Check _hold_start() logic for split-tag handling — verify both tag.startswith(partial) AND partial.startswith(tag) checks exist. 2) Test with simulated multi-chunk streaming using standalone script (not inline python -c which gets intercepted by the live parser). 3) Check for undefined variables in handlers (grep for suspicious names). 4) Verify syntax with py_compile before declaring fix. 5) NEVER restart sable during active session."
    },
    {
      "key": "Deploying phantom_bot webhook to Render",
      "value": "To deploy phantom_bot with webhook on Render: Set env vars IS_LOCAL=False, USE_WEBHOOK=True, WEBHOOK_URL=https://app-name.onrender.com, WEBHOOK_PORT=8443 (or Render-assigned port). Render auto-provisions SSL on .onrender.com. Free tier caveat: spins down after inactivity, Telegram webhooks don't count as keep-alive activity — use paid tier or external uptime pinger. Bot token is used as webhook URL path automatically."
    },
    {
      "key": "Debugging phantom_bot cloudflare tunnel URL-prefix issues",
      "value": "When phantom_bot pages fail under a URL prefix (e.g., /panel), check: 1) JS files for hardcoded absolute paths (fetch, location.href) that lack prefix — inject url_prefix via template. 2) Server-side routes that render templates must pass url_prefix to enable JS prefixing. 3) Regex rewriter (for HTML attrs) must avoid double-prefixing paths already generated by url_for. 4) Homepage slowness: MongoDB Atlas cold-start can cause 502 if index route makes many parallel queries; consider connection warming or query reduction."
    }
  ],
  "ephemeral": []
}