
{
  "name": "MCP Tools",
  "key": "mcp",
  "version": "1.0.0",
  "category": "core",
  "description": "Route calls to external MCP (Model Context Protocol) tool servers over stdio.",
  "trigger": "When the model needs to invoke a tool exposed by a connected MCP server (e.g. github, filesystem, browser).",
  "not_this_if": "The capability is provided by a built-in skill tag instead of an external MCP server.",
  "tags": ["mcp_call"],
  "default": true,
  "inline": false,
  "priority": 50,
  "scope": ["*"],
  "config": {},
  "dependencies": [],
  "$schema": "skill-v1"
}
